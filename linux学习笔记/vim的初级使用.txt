Vim是从 vi 发展出来的一个文本编辑器。

三种模式：
1.一般模式：vi lol.txt进入一般模式
此状态下敲击键盘动作会被Vim识别为命令，而非输入字符。比如我们此时按下i，并不
会输入一个字符，i被当作了一个命令。
以下是常用的几个命令：
i 切换到输入模式，以输入字符。
x 删除当前光标所在处的字符。
: 切换到底线命令模式，以在最底一行输入命令

2.输入模式：在一般模式下输入i进入输入模式
编辑完成后按ESC退回到一般模式

3.底线命令模式：在一般模式下按:进入底线命令模式
在底线命令模式中，基本的命令有（已经省略了冒号）：
q 退出程序
w 保存文件